import 'package:flutter/material.dart';


class Grafic extends StatelessWidget {
  const Grafic({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Column(

      
    );
  }
}